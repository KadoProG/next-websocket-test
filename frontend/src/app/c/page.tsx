import { JoinAndCreateHost } from '@/components/JoinAndCreateHost';
import React from 'react';

const Page = () => (
  <React.Suspense>
    <JoinAndCreateHost />
  </React.Suspense>
);

export default Page;
